# List Data types are immutable
# Lists are hetrogenous and can have different variables like vectors,matrix,lists,dataframes.


l1=list(c(1,2,3,4)); l1
class(l1)
l2=list(c(a=10,b=20,c=30)); l2
class(l2)
l2[1]

student_list <- list(names,weights,genders,phy_marks,chem_marks)
student_list
str(student_list)
